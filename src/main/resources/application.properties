# configuracion del servidor
server.port=8080
spring.application.name=joyeria-api

# PostgreSQL (Neon)
spring.datasource.url=jdbc:postgresql://ep-plain-truth-aivn00tr-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_XK5GRH7rfgSD
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS (ajustar cuando tengas el frontend)
cors.allowed.origins=http://localhost:5173,http://localhost:3000

# Stripe (para despues)
stripe.api.key=${STRIPE_SECRET_KEY:sk_test_dummy}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_dummy}